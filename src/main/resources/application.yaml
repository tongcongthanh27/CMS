server:
  port: 8081
  servlet:
    context-path: /cms
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/cms"
    username: root
    password: 2716
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
