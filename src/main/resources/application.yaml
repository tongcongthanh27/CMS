server:
  port: 8080
  servlet:
    context-path: /cms
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
    username: root
    password: 2716
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
